// Primitives, data structures, structs, and traits are unchanged.

let b: i64 = 2;

let c: f64 = 3.0;

let d: bool = true;

let e: string = "hello";

let f: Array<i64> = [1, 2, 3];


let division = c / 0.15;

let sum = b + c;

let product = b * c;

let difference = b - c;

let remainder = b % c;

let power = b ** c;

let lessThan = b < c;

let lessThanOrEqual = b <= c;

let greaterThan = b > c;

let greaterThanOrEqual = b >= c;

let equal = b == c;

let notEqual = b != c;

let and = b && c;

let or = b || c;

let not = !(b || c);

struct s {

    a: i64,

    b: f64,

}



trait tr {

    add(a: i64, b: f64) -> f64;

}



impl tr for s {

    add(a: i64, b: f64) -> f64 {

        return a + b;

    }

}



let mut counter = 0;

while counter < 10 {

    counter = counter + 1;

}



// NEW: Added examples for if/else control flow.



// A standard if/else statement for conditional execution.

let someValue = 25;

if someValue > 50 {

    printf("Value is large\n");

} else if someValue > 20 {

    printf("Value is medium\n");

} else {

    printf("Value is small\n");

}



// If/else can be used as an expression, assigning the result to a variable.

// The last expression in a block is its return value.

let isEven = if someValue % 2 == 0 {

    true

} else {

    false

};



// Named functions now use a declaration syntax similar to structs.

fn basicFn(a: i64, b: f64) -> f64 {

    return a + b;

}



// Effects are now declared with a name, like a struct or trait.

effect eff {

    Write(string) -> none,

}



// Handlers are also declared with a name.

handler effHandler for eff {

    Write(input:string) -> none {

        printf("writing %s\n", input);

    }

}



// The `let` binding syntax is kept for anonymous/first-class functions.

let myFn = fn(input: string) -> string / {eff} {

    perform eff.Write(input);

    return input;

};



// `handle` syntax remains the same.

let handleResult = handle myFn with effHandler;



// Inline handlers also remain the same.

let handleResult2 = handle myFn with {

    Write(input:string) -> none {

        printf("new writing %s\n", input);

    }

};



// External functions, enums, and other expressions are unchanged.

let externalFn = extern fn strlen(s: string) -> int64;



let expressionResult = {

    let a = 1;

    let b = 2;

    a + b

};



enum myEnum {

    A,

    B(i64),

    C(f64),

}



let myEnumInstance = myEnum::A;



let matchResult = match myEnumInstance {

    A => 1,

    B(a) => a,

    C(a) => a,

};







you're going to write a lexer, parser and type checker for this language. You will first give me a single file. token.rs which has all the tokens we need. next I will ask you to give me a lexer.rs file.
afterwards we move to the parser. parser.rs. If there is any library that would make this easier, faster, please let me know before I start.
